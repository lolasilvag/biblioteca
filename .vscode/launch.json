//{
//pagina que vai abrir o projeto
   /* "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome  localhost",
           // "url": "http://localhost:5000",
         //   "webRoot": "${workspaceFolder}"
            "port:":5000
        }
    ]
}*/

{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch index.js",
        "file": "${workspaceFolder}/index.js"
      }
    ]
  }